on:
  push:
    branches:
      - main
      - dev
      - "refactor/*"
  pull_request:
    branches:
      - main
      - dev

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ... (keep existing but we won't actually push now)